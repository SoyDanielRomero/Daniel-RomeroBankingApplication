name: React CI

on:
  push:
    branches:
      - 'main'

jobs:
  build:
    runs-on: windows-latest

  strategy:
    matrix:
      node-version: [14.x]

  steps:
    - uses: actions/checkout@main
    - run: npm install
    - run: npm run build
    - run: npm test
